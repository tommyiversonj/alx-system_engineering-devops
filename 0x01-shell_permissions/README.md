0x01-shell_permissions
The shell permissions directory is developed based on using shell scripting commands.  
Every file and directory under UNIX or Linux has a set of permissions associated with it that is shown 
as a three digit number (such as 755). These permissions are categorized into three groups who have or do not have the permissions:
